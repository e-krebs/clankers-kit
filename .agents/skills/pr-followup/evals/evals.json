{
  "skill_name": "pr-followup",
  "evals": [
    {
      "id": 1,
      "prompt": "Profile: Ticket system Jira (ACME), Git source GitHub (acme), Default branch develop, CI watcher CircleCI, PR shape draft-first squash CODEOWNERS review, Remote yes. I just ran git push on feature/ACME-1234-facet-sort — watch CI and follow up on the PR.",
      "expected_output": "The widgets-like path: resolve the PR read-only, derive ACME-1234 and the in-review move through the profile's Tracker rules, make one batched offer with the move as a separate question, then watch and re-gather after the watch exits.",
      "assertions": [
        "Reads the workflow profile rows from context and sees a CI watcher, so the watch path applies",
        "Runs gh pr view to resolve the PR before anything else",
        "Derives ACME-1234 through the detection order in the profile's Tracker rules, preferring the PR body",
        "Resolves the move to the in-review state per those rules rather than restating the transition mechanics",
        "Makes exactly one offer that gates the whole chain, with the tracker move as a separately declinable question",
        "Watches nothing and mutates nothing until the user picks Proceed",
        "Re-gathers statusCheckRollup after the watch exits and re-watches when checks are still pending or newly appeared"
      ]
    },
    {
      "id": 2,
      "prompt": "Profile: Ticket system none, Git source GitHub (acme), Default branch main, CI watcher none, PR shape n/a, Remote yes. I just pushed to main — follow up.",
      "expected_output": "With CI watcher none there is nothing to watch: it confirms the push landed in one line and stops, with no offer, no watch and no tracker move.",
      "assertions": [
        "Confirms the push landed — branch, commits, and a PR link only if one exists",
        "Stops there because the CI watcher row is none: no watch, no offer, no re-request",
        "Makes no tracker call, because Ticket system is none",
        "Does not invent a CI provider or poll gh pr checks anyway"
      ]
    },
    {
      "id": 3,
      "prompt": "Profile: Ticket system Jira (ACME), Default branch develop, CI watcher CircleCI, PR shape draft-first squash CODEOWNERS review, Remote yes. CI just went green on PR #451 — it's a draft with ACME-2201 linked and I already approved the follow-up. Advance it.",
      "expected_output": "Marks the draft ready, reports the reviewers the forge auto-assigned without adding any by hand, moves ACME-2201 to the in-review state best-effort, then names the merge capability as the next step.",
      "assertions": [
        "Marks the draft ready with gh pr ready",
        "Reports the auto-assigned reviewers, both team .slug and user .login, without adding reviewers manually",
        "Performs the move of ACME-2201 to the in-review state after the forge action, per the profile's Tracker rules and best-effort",
        "Does not merge the PR",
        "Ends by naming the merge capability as the next step, leaving the invocation to the user"
      ]
    },
    {
      "id": 4,
      "prompt": "Profile: Ticket system Jira (ACME), Default branch develop, CI watcher CircleCI, Remote yes. CI failed on my PR after the push. What now?",
      "expected_output": "Reports the failing checks, summarizes the failing logs, gives a short root-cause read, then stops and asks whether to investigate — with no tracker move on red.",
      "assertions": [
        "Reports which checks failed and summarizes the failing logs via gh run view --log-failed",
        "Gives a short root-cause read",
        "Stops and asks before doing anything — no auto-fix and no re-push",
        "Performs no tracker move on red",
        "Does not name the merge capability, since the PR is not green"
      ]
    },
    {
      "id": 5,
      "prompt": "Profile: Ticket system Jira (ACME), Default branch develop, CI watcher CircleCI, Remote yes. I ran a git push but this branch has no PR open. Follow up.",
      "expected_output": "Runs the read-only resolve, finds no associated PR, and exits silently with no output.",
      "assertions": [
        "Attempts to resolve the PR with a single gh pr view",
        "Exits silently with no output when no PR exists",
        "Makes no tracker call and no offer",
        "Exits the same way on a merged or closed PR"
      ]
    },
    {
      "id": 6,
      "prompt": "Profile: Ticket system none, Git source GitHub (acme), Default branch main, CI watcher GitHub Actions, PR shape squash, Remote yes. Green CI on open PR #77, which already has reviews from @alice and @bob (bob's re-review is already pending). Follow up.",
      "expected_output": "The watch path runs because a CI watcher is named, only @alice is re-requested, and no ticket question or move appears at all since Ticket system is none.",
      "assertions": [
        "Runs the watch path because the CI watcher row names GitHub Actions, using gh pr checks as for any other CI",
        "Re-requests @alice via gh pr edit --add-reviewer",
        "Excludes @bob, whose re-review request is already pending, along with bots and the PR author",
        "Asks no ticket question and performs no tracker move, because Ticket system is none",
        "Does not mark ready — the PR is already open, not a draft",
        "Ends by naming the merge capability as the next step"
      ]
    },
    {
      "id": 7,
      "prompt": "Profile: Ticket system Jira (ACME), Git source GitHub (acme), Default branch main, CI watcher GitHub Actions, PR shape 'draft-first, squash, gh stack for a multi-PR change, no required approval', Remote yes. I just ran gh stack submit. The stack is feat/base (PR #11, draft, ACME-3001), feat/api (PR #12, draft, ACME-3002) and feat/ui (PR #13, draft, ACME-3003), and I'm standing on feat/ui.",
      "expected_output": "Reads references/gh-stack-followup.md in full, enumerates all three members from gh stack view --json plus one gh pr view each, offers one gate naming the whole stack, watches all three PRs' checks, and on green advances only #11 — marks it ready, reports the reviewers the forge assigned, and moves ACME-3001 alone — reporting #12 and #13 by number with why they wait.",
      "assertions": [
        "Enumerates the members with gh stack view --json before resolving any single PR",
        "Does not resolve only the current branch's PR, even though the user is standing on feat/ui",
        "Watches every live member's checks, not just the current branch's, because a stack push republishes every branch",
        "Applies the staged-CI re-gather per member and treats a pending check as blocking, not as green",
        "Advances only #11, the bottom open member, and leaves #12 and #13 as drafts",
        "Moves exactly one ticket, ACME-3001, and names ACME-3002 and ACME-3003 as moving when their own PR reaches review",
        "One offer gates the whole chain, with the single tracker move as a separately declinable question",
        "Names the merge capability as the next step and says the stack is reviewed bottom-up"
      ]
    },
    {
      "id": 8,
      "prompt": "Profile: Ticket system Jira (ACME), Git source GitHub (acme), Default branch main, CI watcher GitHub Actions, PR shape 'draft-first, squash, gh stack for a multi-PR change', Remote yes. I just pushed feat/solo, whose PR targets main. gh stack view reports it is not part of a stack, and no open PR targets feat/solo.",
      "expected_output": "The row says the repo stacks; the command says this branch does not. The PR walk runs the --base query anyway, because a bottom-of-stack PR also targets the trunk, finds no children, and the run then takes the ordinary single-PR path without mentioning stacks to the user.",
      "assertions": [
        "The PR shape row alone does not make the run treat one branch as a stack",
        "The --base query still runs even though the PR's base is the Default branch, because that is the bottom-of-stack case",
        "Finding no children closes the path file and takes Step 0's single-PR path",
        "Exactly one gh pr checks --watch runs, for the one PR",
        "No gh stack command other than the single gh stack view --json is issued"
      ]
    }
  ]
}
