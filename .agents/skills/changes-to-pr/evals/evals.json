{
  "skill_name": "changes-to-pr",
  "evals": [
    {
      "id": 1,
      "prompt": "Profile: Ticket system Jira (ACME), Git source GitHub (acme), Default branch develop, Commit policy PRs never direct, Commit convention conventional w/ scope + ticket ref in PR body only, CI watcher CircleCI, PR shape draft-first squash CODEOWNERS review, Remote yes. Open a PR for my changes on feat/composition-panel — no linked ticket yet.",
      "expected_output": "The widgets-like path, unchanged in behaviour: inspect, one batched Gate 1 (ticket, branch target, split, review), a plan with atomic conventional-scope commits and a template-based description, Gate 2, then a draft PR and a hand-off line naming the CI watch.",
      "assertions": [
        "Reads the workflow profile rows from context before asking anything",
        "Runs the git inspection (branch, base develop, status, diff, branch lead) first",
        "Asks about the ticket because Ticket system names a tracker, detecting the id through the profile's Tracker rules detection order",
        "Commits nothing and pushes nothing before Gate 2 approval",
        "Uses conventional commit subjects with a scope and no ticket id, putting the reference in the PR body only, as the Commit convention row says",
        "Creates the PR with --draft because the PR shape row is draft-first",
        "Closes by saying a hook hands off to the CI watch, since the CI watcher row names CircleCI",
        "Names no other skill anywhere in the run"
      ]
    },
    {
      "id": 2,
      "prompt": "Profile: Ticket system none, Git source GitHub (acme), Default branch main, Commit policy direct commits to main, Commit convention conventional w/ scope, CI watcher none, PR shape n/a, Remote yes. Commit and PR this.",
      "expected_output": "Under direct commits to main it commits on the default branch and stops: no branch, no PR, no ticket question, and it says plainly that no CI watch follows.",
      "assertions": [
        "Honors Commit policy direct commits to main — commits on main with no branch created and no PR opened",
        "Asks no ticket question and performs no tracker move, because Ticket system is none",
        "Still splits the work into atomic conventional-scope commits, one per concern, gated at Gate 2",
        "Drafts no PR description",
        "Says plainly that no CI watch follows, since the CI watcher row is none",
        "Explains the direct-commit choice rather than silently ignoring the PR half of the request"
      ]
    },
    {
      "id": 3,
      "prompt": "Profile: Ticket system none, Git source none, Default branch main, Commit policy PRs never direct, Commit convention conventional w/ scope, CI watcher none, PR shape n/a, Remote local-only. Ship these changes.",
      "expected_output": "With Remote local-only the run ends at the commits: it builds the split, gates it, commits, and reports that nothing was pushed and no PR exists to open.",
      "assertions": [
        "Stops at the commits because Remote is local-only — no git push and no gh pr create",
        "Reports explicitly that nothing was pushed rather than failing on a missing remote",
        "Skips the branch-target question that only applies to a remote PR flow",
        "Asks no ticket question and says no CI watch follows"
      ]
    },
    {
      "id": 4,
      "prompt": "Profile: Ticket system Jira (ACME), Default branch develop, Commit policy PRs never direct, Commit convention conventional w/ scope + ticket ref in PR body only, CI watcher CircleCI, PR shape draft-first squash CODEOWNERS review, Remote yes, Reviewers claude and codex. The approved plan is in context: its Closing steps line reads 'review: judgment tier x 2' and it prescribes two commits. Cut the PR for ACME-2258.",
      "expected_output": "It takes the tier and the count from the plan's Closing steps line, delegates to the review capability read-only, applies the findings it accepts itself and re-reads the diff, follows the plan's prescribed commit split, and asks the tracker move batched into Gate 2.",
      "assertions": [
        "Reads the tier and reviewer count from the plan's Closing steps review line rather than offering its own",
        "Delegates to the review capability with exactly that tier and count",
        "Treats reviewers as read-only — the main agent applies the accepted findings and no reviewer edits the tree",
        "Re-reads the working-tree diff after applying the accepted fixes, before building the split and the description",
        "Follows the commit split the plan prescribed instead of regrouping by its own analysis",
        "Batches the move of ACME-2258 to the work-started state into the Gate 2 interaction, best-effort on failure",
        "Adds an Agent review section to the PR body naming coverage, findings, verdict, and which findings were applied"
      ]
    },
    {
      "id": 5,
      "prompt": "Profile: Ticket system Jira (ACME), Default branch develop, Commit policy PRs never direct, CI watcher CircleCI, PR shape draft-first squash CODEOWNERS review, Remote yes. Commit and PR this — the diff mixes a bugfix and an unrelated refactor. Split them.",
      "expected_output": "Detects the two concerns, confirms the split at Gate 1, then runs the build-and-execute steps once per concern, presenting all per-concern plans at a single Gate 2, with the tracker move running once up front.",
      "assertions": [
        "Detects the two concerns from the diff and asks whether to proceed as one PR or split",
        "Produces one branch, its atomic commits, and one PR per concern",
        "Presents the per-concern plans together at a single Gate 2",
        "Runs the tracker move once up front for the single ticket, not once per PR",
        "Bases each concern's branch per the Gate 1 choice — independent off develop, or stacked"
      ]
    },
    {
      "id": 6,
      "prompt": "Profile: Ticket system Jira (ACME), Default branch develop, Commit policy PRs never direct, CI watcher GitHub Actions, PR shape squash CODEOWNERS review, Remote yes. Update the existing PR for this branch with my new commits. It already has a description I wrote by hand.",
      "expected_output": "Recognizes the non-template body, warns at Gate 2 that it would be replaced and offers to keep it, and on approval pushes and updates via gh pr edit; with a PR shape that is not draft-first it opens nothing as a draft, and it still hands off to the CI watch.",
      "assertions": [
        "Detects the existing PR and that its body is non-template",
        "Warns at Gate 2 that the hand-written body would be replaced and offers to keep it",
        "Replaces the body only on explicit approval, via gh pr edit --body-file -",
        "Omits --draft because the PR shape row is not draft-first",
        "Never promotes the PR to ready, merges it, or watches CI itself, closing instead on the hand-off to the CI watch",
        "Aborts a hunk-level commit cleanly on a git apply --cached failure, pushing no partial result"
      ]
    },
    {
      "id": 7,
      "prompt": "Profile: Ticket system none, Git source GitHub (acme), Default branch main, Commit policy PRs never direct, Commit convention conventional w/ scope, CI watcher GitHub Actions, PR shape draft-first squash, Remote yes. I'm on feat/api-client, which has its own open PR against main. Open a PR for my changes — they build on that branch, so stack a new branch on top of it.",
      "expected_output": "Binds <base> to main for the diff and the branch lead, and binds <pr-base> to feat/api-client from the Gate 1 stacked answer. Cuts the new branch off feat/api-client and opens its draft PR with --base feat/api-client, so the PR's diff is the new commits alone.",
      "assertions": [
        "Cuts the branch off feat/api-client, the Gate 1 answer, not off main",
        "Passes --base feat/api-client to gh pr create, so <base> never reaches the PR-create command",
        "Says the PR opens against its parent branch, and presents no diff that includes feat/api-client's own commits",
        "Still measures the Step 0 diff and the branch lead against main, which is what <base> is for",
        "Names the binding it used, so a reader can tell <pr-base> from <base>"
      ]
    },
    {
      "id": 8,
      "prompt": "Profile: Ticket system none, Git source GitHub (acme), Default branch main, Commit policy PRs never direct, Commit convention conventional w/ scope, CI watcher GitHub Actions, PR shape 'draft-first, squash, gh stack for a multi-PR change, no required approval', Remote yes. I'm on main and the tree mixes a schema change and the endpoint that uses it. Ship them as two PRs.",
      "expected_output": "Reads references/gh-stack-path.md in full before any gh stack command, runs its read-only P1 (paused cascade, linked worktree, version, remote, gh stack view --json which reports no stack, the mutation set), carries the init route into Gate 2, then cuts both branches and commits with plain git bottom to top, tracks them with one gh stack init --base main, submits with gh stack submit --auto, applies the Gate 2 titles and bodies with gh pr edit before anything else, and verifies both branches against their remotes.",
      "assertions": [
        "Reads references/gh-stack-path.md in full before running any gh stack command",
        "A single-concern run under the same row would take Step 4's plain items instead, because one PR is not a multi-PR change",
        "Runs gh stack view --json, never the bare gh stack view, which opens a TUI that never returns",
        "Tests for a linked worktree before believing a 'not part of a stack' answer, and degrades rather than calling init from one",
        "Creates both branches and their atomic commits with plain git before anything tracks the stack, so gh stack is never handed a dirty tree",
        "Passes --base main to gh stack init and nowhere else on the path",
        "Runs gh stack submit with --auto and without --open, because the PR shape row is draft-first",
        "Passes no -A, -u or -m to gh stack add, which would collapse the split into one commit",
        "Applies the Gate 2 title and body per PR with gh pr edit, because submit --auto invents both and has no --title or --body",
        "Runs no git push -u of its own, since submit pushes the branches",
        "Verifies every branch in the mutation set against its remote after the submit, the created ones and the existing members, because the push is not atomic",
        "Closes on the hand-off to the CI watch, saying it watches every member and advances the bottom open one"
      ]
    },
    {
      "id": 9,
      "prompt": "Profile: Ticket system none, Git source GitHub (acme), Default branch main, Commit policy PRs never direct, Commit convention conventional w/ scope, CI watcher GitHub Actions, PR shape 'squash, gh stack for a multi-PR change', Remote yes. I'm on feat/groundwork, which has no PR yet. Commit and PR this — the diff mixes a refactor, a feature that depends on it, and a doc update. Split them, stacked.",
      "expected_output": "Detects three concerns, pre-selects the stacked answer because the row carries the clause, then builds each concern's branch and commits bottom to top with plain git, tracks the whole set with one gh stack init, submits once with gh stack submit --auto, applies the approved titles and bodies, and only then marks each created PR ready with gh pr ready rather than passing --open.",
      "assertions": [
        "The stacked option is offered first and pre-selected, and the question is still asked",
        "Each concern's branch is cut from the concern below it, and its PR opens against that same branch, so the Nth PR's diff is the Nth concern alone",
        "One gh stack init lists every created branch bottom to top, with feat/groundwork as the bottom member, rather than one init or add per concern",
        "One gh stack submit covers the whole set, rather than one submit per concern",
        "gh pr ready runs per created PR after the title-and-body pass, never submit --open, which would also promote an existing deliberately-draft member",
        "All three per-concern plans are presented at a single Gate 2, which names the stack and the whole mutation set",
        "gh stack view --json after the submit is what maps each branch to its PR number",
        "The stack is tracked once and submitted once, rather than per concern"
      ]
    }
  ]
}
